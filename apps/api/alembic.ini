[alembic]
# sqlalchemy.url is deliberately absent: migrations/env.py reads DATABASE_URL
# from the app settings so there is a single source of truth.
script_location = migrations
prepend_sys_path = .
path_separator = os